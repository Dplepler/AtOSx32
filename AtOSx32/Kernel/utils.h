#ifndef UTILS_H
#define UTILS_H
#include <stddef.h>

char* itoa(int value, size_t base);
char* strcat(char* dest, const char* src);
char* strcat(char* dest, const char* src);

size_t strl(const char* str);
size_t intl(int n);


#endif
